{
  "name": "todoist-node-api",
  "description": "A node wrapper for the todoist sync API",
  "version": "0.1.0",
  "keywords": [
    "Todoist",
    "Todoist API"
  ],
  "main": "index.js",
  "dependencies": {
    "request": "^2.75.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-core": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "jshint": "^2.9.4",
    "mocha": "^3.1.2",
    "nsp": "^2.6.2",
    "request": "^2.76.0",
    "rimraf": "^2.5.4"
  },
  "scripts": {
    "compile": "rimraf lib/ && babel -d lib/ src/",
    "mocha": "mocha --opts mocha.opts || exit 0",
    "test": "npm run compile && nsp check && npm run mocha"
  },
  "author": "",
  "license": "MIT"
}
